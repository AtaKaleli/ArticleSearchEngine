Article search engine using Client-Server management system. The GUI created by python tkinter modele. Also the system is a multi-threaded system, so multiple users can use the system at the same time.
